export enum ProjectPriorityEnum {
  Low = 0,       // Minor / optional projects, less impact
  Medium = 1,    // Normal priority projects
  High = 2,      // Important projects, should be delivered on time
  Critical = 3,  // Mission-critical projects, high visibility and risk
}
